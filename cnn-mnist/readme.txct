#输入图片32*32*1#32行*32列*单通道
卷积核5*5*1*6#5行5列*单通道*6个卷积核
#非0 步长为1 即用（32-5+1）/1=28步
即输出的点阵为28*28*6深度（每一个核对应一层深度）
#pooling池化操作 #变成14*14*6#池化核为2*2#池化不改变深度
#卷积核5*5*6*16每一个核都有5*5*6的大小每一个深度为6*一共16个核
#为（14-5+1）/1=10 故第二次卷积之后的大小为10*10*16 16为深度
#并进行激活函数
#继续用2*2核进行池化
#最后得到5*5*16

28*28*1  || 5*5*1*32
28*28*32（使用补0操作）（即全0填充）
2*2pooling
14*14*32
14*14*32 || 5*5*32 *64
14*14*64
2*2pooling
7*7*64
全连接

